{
	"description": "PUG example of bun-bundler usage.",
	"author": "jsqrt",
	"license": "MIT",
	"main": "index.js",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/jsqrt/bun-bundler.git"
	},
	"devDependencies": {
		"bun-bundler": "file:../../"
	},
	"scripts": {
		"start": "bun dev",
		"dev": "cross-env NODE_ENV=development bun run dev-scripts/dev.js",
		"build": "cross-env NODE_ENV=production bun run dev-scripts/build.js"
	},
	"dependencies": {
		"cross-env": "^7.0.3"
	}
}
